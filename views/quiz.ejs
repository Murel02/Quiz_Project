<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
<header>
    <nav>
        <a href="/home">Home</a>
        <a href="/leaderboard">Leaderboard</a>
        <a href="/logout">Logout</a>
    </nav>
</header>
<div class="container">
    <% if (question) { %> <!-- Display the current question -->
    <h1><%= quiz.title %></h1>
    <h3>Question <%= questionIndex + 1 %>: <%= question.question %></h3>
    <form action="/quiz/<%= quiz._id %>/submit" method="POST">
        <% question.options.forEach(option => { %>
            <label>
                <input type="radio" name="selectedAnswer" value="<%= option %>" required>
                <%= option %>
            </label>
        <% }) %>
        <button type="submit" class="button">Submit Answer</button>
    </form>
    <% } else { %> <!-- If no question is available, quiz is finished -->
    <h1>Quiz Completed!</h1>
    <p>Thank you for participating in the quiz.</p>
    <a href="/home">Back to Home</a>
    <% } %>
</div>
</body>
</html>
