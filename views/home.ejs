<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to QuizMaster</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
<header class="header">
    <a href="/home" class="logo">
        <img src="/images/pixilart-drawing.png" alt="QuizMaster Logo" class="logo-image">
    </a>
    <h1 class="site-title">Welcome to QuizMaster!</h1>
    <nav class="nav">
        <% if (isLoggedIn) { %>
            <a href="/logout" class="nav-link">Logout</a>
        <% } else { %>
            <a href="/login" class="nav-link">Login</a>
            <a href="/register" class="nav-link">Register</a>
        <% } %>
    </nav>
</header>

<main class="main">
    <div class="container">
        <h1 class="main-title">Test Your Knowledge</h1>
        <p class="main-description">Ready to challenge yourself? Start a quiz now!</p>
        <div class="actions">
            <form action="/quiz/create" method="POST">
                <button type="submit" class="button">Start Quiz</button>
            </form>
        </div>

        <!-- Mini Leaderboard -->
        <section class="leaderboard-preview">
            <h2 class="leaderboard-title">Top Players</h2>
            <table class="leaderboard-table">
                <thead>
                <tr>
                    <th>Name</th>
                    <th>Score</th>
                </tr>
                </thead>
                <tbody>
                <% topPlayers.forEach(player => { %>
                    <tr>
                        <td><%= player.name %></td>
                        <td><%= player.score %></td>
                    </tr>
                <% }) %>
                </tbody>
            </table>
            <a href="/leaderboard" class="button">View Full Leaderboard</a>
        </section>
    </div>
</main>
<footer class="footer">
    <p>&copy; 2024 QuizMaster. All rights reserved.</p>
</footer>
</body>
</html>
